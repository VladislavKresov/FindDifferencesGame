public class LevelStartSignal {
    public readonly int Level;
    public readonly float TimeLimit;

    public LevelStartSignal(int level, float timeLimit) {
        Level = level;
        TimeLimit = timeLimit;
    }
}
